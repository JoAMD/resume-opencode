---
phase: 01-resume-json-diff-viewer
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - services/backupService.ts
  - routes/generate.ts
  - routes/generate.test.ts
autonomous: true
requirements:
  - DIFF-02
  - DIFF-03
  - DIFF-04
  - DIFF-04b
  - DIFF-06
  - DIFF-07b
must_haves:
  truths:
    - "GET /generate/diffResume?jobDir=<slug>&version=<vN>&format=unified|summary|both returns 200 on a known job with a known backup."
    - "The route reuses safeRealpath + ensureJobsRootRealpath from routes/generate.ts:609-622 to allowlist jobDir; path-traversal payloads (jobDir=../etc) are rejected (400 or 500 per the existing pattern)."
    - "jobDir missing returns 400 { error: 'jobDir required' }."
    - "version matching /v0/, /v-1/, /vabc/ returns 400 { error: 'invalid version' }."
    - "When version is omitted, the route defaults to latestBackupVersion(backupsRoot)."
    - "Missing backup returns 404 { error: 'Backup not found', jobDir, version }; missing current returns 404 { error: 'Resume not found', jobDir, version }."
    - "format=unified omits summary; format=summary omits unifiedDiff; format=both (default) includes both."
    - "Both files are pretty-printed via JSON.stringify(_, null, 2) before diffing so key reordering does not show as noise."
    - "The new GET /generate/diffResume route is registered on the existing /generate router (no requireAdminAuth wrap), matching the codebase convention that no /generate/* route is admin-gated today (only /api/config and /api/edit are wrapped per server.ts:65 and server.ts:139)."
    - "services/backupService.ts adds latestBackupVersion(backupsRoot): number | null as a pure read; no behaviour change to nextBackupVersion."
    - "npm test and npm run build are clean; the new route tests in routes/generate.test.ts pass."
  artifacts:
    - services/backupService.ts (edited: add latestBackupVersion(backupsRoot) export)
    - routes/generate.ts (edited: add GET /diffResume route handler, query validators, import diffUtil, import latestBackupVersion)
    - routes/generate.test.ts (edited: add describe('GET /generate/diffResume', ...) block with happy path, 404s, 400s, traversal, format variants)
  key_links:
    - "routes/generate.ts imports unifiedDiffText and summariseJsonDiff from ../services/diffUtil (the module Plan 1 created)."
    - "routes/generate.ts imports latestBackupVersion from ../services/backupService (this plan's Task 1)."
    - "The route handler reuses the same safeRealpath + ensureJobsRootRealpath + pathIsInsideDir pattern as the existing POST /applySuggestions handler (lines 840-841 and 691-699) — the path-allowlist is the same."
    - "Routes/generate.test.ts is co-located next to routes/generate.ts; new describe block is appended."
---

<objective>
Step 2 of `docs/plans/RESUME_DIFF_VIEWER_PLAN.md`: add the read-only `GET /generate/diffResume` route and the supporting `latestBackupVersion(backupsRoot)` helper. The route returns the unified diff between a chosen backup version and the on-disk resume, plus a `summary.changedPaths` list, when both files exist on disk. The route is path-allowlisted (reuses `safeRealpath` + `ensureJobsRootRealpath` from `routes/generate.ts:609-622`), validates `version` against `/^v\d+$/` after `Number` parse, defaults to the latest backup when `version` is omitted, and accepts a `format` query (`unified` | `summary` | `both`, default `both`).

This plan depends on Plan 1: the route imports `unifiedDiffText` and `summariseJsonDiff` from `../services/diffUtil`. No new state — the diff is computed on disk from the same `backups/vN/structured-output.json` snapshot already produced by `applySuggestions` and the on-disk `structured-output.json`.

The plan also adds `latestBackupVersion(backupsRoot): number | null` to `services/backupService.ts`. The new helper is a pure read; it does not change `nextBackupVersion` semantics. Both `nextBackupVersion` and `latestBackupVersion` are read from the same `/^v(\d+)$/` directory naming convention.
</objective>

<execution_context>
@/home/adf_home_joel/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/adf_home_joel/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/research/DIFF_LIBRARY_NOTE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@docs/plans/RESUME_DIFF_VIEWER_PLAN.md
@.planning/phases/01-resume-json-diff-viewer/01-01-PLAN.md

# Files this plan modifies (read before editing)
@services/backupService.ts (the file to add latestBackupVersion to; mirror the nextBackupVersion /v(N)/ parse)
@services/diffUtil.ts (Plan 1's output — the new module this route imports)
@routes/generate.ts (lines 609-622 for safeRealpath/ensureJobsRootRealpath, lines 642-684 for the validation discriminated union, lines 788-831 for the applySuggestions pattern this route mirrors, lines 833-859 for the POST /applySuggestions handler this route parallels)
@routes/generate.test.ts (lines 1-80 for the mock setup; the file uses vi.mock for fs, child_process, paths, latex, env, ai, fixSuggestionsService, backupService)
@server.ts (lines 40-52 for the requireAdminAuth middleware — the route is intentionally NOT wrapped in admin auth per the plan's Step 2 note)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add latestBackupVersion helper to services/backupService.ts
  <read_first>
    - services/backupService.ts (lines 22-33 are the existing nextBackupVersion; mirror its parse logic)
    - docs/plans/RESUME_DIFF_VIEWER_PLAN.md (Step 2.2 — the latestBackupVersion contract)
    - services/backupService.test.ts (lines 6-25 for the fs-mock pattern; lines 81-99 for the nextBackupVersion readdir-iteration style)
  </read_first>
  <action>
    In `services/backupService.ts`, add a new exported function `latestBackupVersion(backupsRoot: string): number | null` immediately after the existing `nextBackupVersion` function (after line 33). Implementation:

    ```ts
    export function latestBackupVersion(backupsRoot: string): number | null {
      if (!fs.existsSync(backupsRoot)) return null;
      const entries = fs.readdirSync(backupsRoot);
      let max = 0;
      let found = false;
      for (const entry of entries) {
        const match = entry.match(/^v(\d+)$/);
        if (!match) continue;
        const n = parseInt(match[1], 10);
        if (Number.isFinite(n) && n > max) {
          max = n;
          found = true;
        }
      }
      return found ? max : null;
    }
    ```

    Rules:
    - This is a pure read; do NOT call `fs.mkdirSync` or `fs.copyFileSync`.
    - Mirror the `nextBackupVersion` regex (`/^v(\d+)$/`) and `parseInt(match[1], 10)` parse so the two helpers agree on the directory naming.
    - The `found` flag distinguishes "directory exists but no vN folders" (returns `null`) from "directory missing" (returns `null`); both return `null` per the plan's contract — callers should treat both as "no version".
    - Do NOT change `nextBackupVersion` behaviour; that function is called from `createVersionedBackup` (line 54).
    - Add no new dependencies without evaluation. Use only the existing `fs` import (already at the top of the file).
    - The new function is a named export added to the file's export list — no default export.
  </action>
  <verify>
    <automated>cd /home/adf_home_joel/src/copilot/resume-opencode && npx tsc --noEmit services/backupService.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `services/backupService.ts` exports a new `latestBackupVersion(backupsRoot: string): number | null` function.
    - [ ] `latestBackupVersion` is a pure read: it calls only `fs.existsSync` and `fs.readdirSync`; no `mkdirSync`, no `copyFileSync`.
    - [ ] `nextBackupVersion` is unchanged (same regex, same parse, same return).
    - [ ] `npx tsc --noEmit services/backupService.ts` exits 0 with no errors.
    - [ ] `grep -n "latestBackupVersion" services/backupService.ts` shows the new function definition and export.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add GET /generate/diffResume route to routes/generate.ts
  <read_first>
    - routes/generate.ts (lines 609-622 for safeRealpath, pathIsInsideDir, ensureJobsRootRealpath; lines 642-684 for the validation discriminated union; lines 788-831 for the buildApplySuggestionsResult pattern; lines 833-859 for the POST /applySuggestions pattern this route mirrors)
    - services/diffUtil.ts (Plan 1 output — exports unifiedDiffText and summariseJsonDiff)
    - services/backupService.ts (Task 1's output — exports latestBackupVersion)
    - server.ts (lines 40-52 confirm that admin auth is reserved for /api/config and /api/edit, not for the /generate sub-router; this new route stays under the existing /generate mount without requireAdminAuth per the plan)
    - .planning/codebase/CONVENTIONS.md (no default exports, single-quote strings, 2-space indent, semicolons, named export pattern)
    - docs/plans/RESUME_DIFF_VIEWER_PLAN.md (Step 2.1 — the response shape and the validation rules)
  </read_first>
  <action>
    In `routes/generate.ts`:

    1. Add two new imports near the top of the file (alphabetised with the other relative imports, after the existing `applySuggestions, AttachedFile, NoOpResultError` import on line 19):
       ```ts
       import { unifiedDiffText, summariseJsonDiff } from '../services/diffUtil';
       import { latestBackupVersion } from '../services/backupService';
       ```
       (No `.js` extension; CONVENTIONS.md allows source-to-source imports without it.)

    2. Add a `validateDiffResumeRequest(req): ValidationFailure | ValidationSuccess<{ jobDir: string; backupVersion: number; format: 'unified' | 'summary' | 'both' }>` helper function near the other `validate*Request` helpers (after `validateEnsureRedactedResumeRequest` at line 755-765 is a reasonable spot). The helper does:

       a. Read `req.query.jobDir`. If absent or not a non-empty string, return `{ ok: false, status: 400, error: 'jobDir required' }`. (Note: the wording is "jobDir required" per DIFF-04b; not "jobDir is required" — match exactly.)

       b. Resolve `realJobDir = safeRealpath(jobDir)` and `realJobsRoot = ensureJobsRootRealpath()`. If either is null, return `{ ok: false, status: 500, error: 'Failed to resolve job directory' }` (same shape as the existing /applySuggestions 500 on the same condition, line 843).

       c. If `!pathIsInsideDir(realJobDir, realJobsRoot)`, return `{ ok: false, status: 400, error: 'jobDir escapes jobs root' }` (path-traversal payload rejected).

       d. Read `req.query.version`. If absent, call `latestBackupVersion(path.join(realJobDir, 'backups'))`. If that returns `null`, return `{ ok: false, status: 404, error: 'Backup not found', jobDir, version: null }`. Otherwise, store the resulting integer in `backupVersion`.

       e. If `version` is present, parse as a string. Strip a leading `v` if present, parse the rest as a positive integer, and require the result to be `>= 1`. Reject `v0`, `v-1`, `vabc`, empty after strip, or anything that is not a positive integer — return `{ ok: false, status: 400, error: 'invalid version' }`. The original string is also kept so the response can echo the rejected value when applicable. The regex is `/^v\d+$/` AFTER Number-parse succeeds; both checks must pass.

       f. Read `req.query.format`. If absent, default to `'both'`. If present, it must be one of `'unified' | 'summary' | 'both'`. Anything else → `{ ok: false, status: 400, error: 'invalid format' }`.

       g. Resolve `backupsRoot = path.join(realJobDir, 'backups')` and `backupFile = path.join(backupsRoot, \`v${backupVersion}\`, 'structured-output.json')`. If `!fs.existsSync(backupFile)`, return `{ ok: false, status: 404, error: 'Backup not found', jobDir, version: \`v${backupVersion}\` }`.

       h. Resolve `currentFile = path.join(realJobDir, 'structured-output.json')`. If `!fs.existsSync(currentFile)`, return `{ ok: false, status: 404, error: 'Resume not found', jobDir, version: \`v${backupVersion}\` }`.

       i. Return `{ ok: true, value: { realJobDir, jobDir, backupVersion, backupFile, currentFile, format } }`.

    3. Add a route handler `diffResumeHandler(req, res)` immediately after the helper:

       a. Call `const validated = validateDiffResumeRequest(req);`. If `isFailure(validated)`, return `res.status(validated.status).json(validated.error.includes('version') || validated.error.includes('Backup') || validated.error.includes('Resume') ? { error: validated.error, jobDir: req.query.jobDir, version: req.query.version } : { error: validated.error })`. (The 404 responses include `{ jobDir, version }`; the 400 responses include just `{ error }`.)

       b. Read both files with `fs.readFileSync`, parse with `JSON.parse`, then `JSON.stringify(parsed, null, 2)` for both before passing to `unifiedDiffText` and `summariseJsonDiff`. Do NOT use `canonicalize` here — the pretty-print is for display and the path list uses `summariseJsonDiff` (which does its own internal comparison; see DIFF-02).

       c. Build the response object:
          ```ts
          const response: Record<string, unknown> = {
            jobDir: validated.value.jobDir,
            backupVersion: validated.value.backupVersion,
            backupPath: validated.value.backupFile,
            currentPath: validated.value.currentFile,
          };
          if (validated.value.format === 'unified' || validated.value.format === 'both') {
            response.unifiedDiff = unifiedDiffText(
              JSON.stringify(JSON.parse(fs.readFileSync(validated.value.backupFile, 'utf8')), null, 2),
              JSON.stringify(JSON.parse(fs.readFileSync(validated.value.currentFile, 'utf8')), null, 2),
              `backup (v${validated.value.backupVersion})`,
              'current'
            );
          }
          if (validated.value.format === 'summary' || validated.value.format === 'both') {
            const backupParsed = JSON.parse(fs.readFileSync(validated.value.backupFile, 'utf8'));
            const currentParsed = JSON.parse(fs.readFileSync(validated.value.currentFile, 'utf8'));
            response.summary = summariseJsonDiff(backupParsed, currentParsed);
          }
          ```
          (Read each file once and cache the parsed value to avoid re-reading. The implementation should parse each file exactly once; the snippet above reads each twice — the executor should hoist the reads into local variables for clarity. The check for DIFF-07b's "format=unified omits summary" is that `response.summary` is undefined in the unified case; same for `unifiedDiff` in the summary case.)

       d. `res.status(200).json(response);`.

    4. Register the route near the other GET routes. The natural spot is right after the `router.get('/redactedResumePath', ...)` at line 769-786, before `function buildApplySuggestionsResult(...)` at line 788. Use:
       ```ts
       router.get('/diffResume', diffResumeHandler);
       ```
       The route is read-only and intentionally NOT wrapped in `requireAdminAuth` (the `/generate` sub-router is mounted on `app.use('/generate', generateRouter)` in `server.ts:300` with no admin gate; admin-gating the diff route is not requested by the source plan and is out of scope per PROJECT.md).

    5. Add a small helper for the `pathIsInsideDir` reuse — the function already exists at line 617. Use it directly; do not duplicate.

    6. File-level rules per CONVENTIONS.md:
       - No default export.
       - The new route handler and the validation helper stay under 60 lines each.
       - Use the existing `log` and `logError` from `services/logger` (already imported on line 8) for the success / failure traces that mirror the `applySuggestions` handler.
       - Use the `ValidationFailure | ValidationSuccess<T>` discriminated union exactly as the rest of the file does — `isFailure(validated)` to narrow.
  </action>
  <verify>
    <automated>cd /home/adf_home_joel/src/copilot/resume-opencode && npx tsc --noEmit routes/generate.ts 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `routes/generate.ts` imports `unifiedDiffText`, `summariseJsonDiff` from `../services/diffUtil` and `latestBackupVersion` from `../services/backupService`.
    - [ ] A new `GET /diffResume` route is registered (visible as `router.get('/diffResume', diffResumeHandler)`).
    - [ ] The validation helper rejects missing `jobDir` with `400 { error: 'jobDir required' }` and bad `version` with `400 { error: 'invalid version' }`.
    - [ ] Path-traversal payloads are rejected by the same `safeRealpath` + `pathIsInsideDir(realJobDir, realJobsRoot)` chain as the existing `/applySuggestions` handler.
    - [ ] 404 responses for missing backup or missing current include `{ error, jobDir, version }` in the body.
    - [ ] When `format=unified` the response has no `summary` key; when `format=summary` it has no `unifiedDiff` key; when `format=both` (default) it has both.
    - [ ] `npx tsc --noEmit routes/generate.ts` exits 0 with no errors.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add route tests to routes/generate.test.ts covering happy path, 404s, 400s, traversal, and format variants
  <read_first>
    - routes/generate.test.ts (lines 1-80 for the mock setup; locate the existing describe blocks and the invokeRoute helper at the bottom of the file)
    - .planning/codebase/TESTING.md (route-test pattern: spin up Express, real http.request, the canonical invokeRoute helper at lines 472-503)
    - routes/generate.ts (the new diffResume route from Task 2; copy the exact response shape and error strings from the action)
    - services/backupService.test.ts (the readdirMock pattern at lines 81-99 to make nextBackupVersion-style reads testable)
  </read_first>
  <action>
    Append a new `describe('GET /generate/diffResume', ...)` block to `routes/generate.test.ts` (after the existing describe blocks; the file ends with `afterEach(() => { vi.clearAllMocks(); });` based on the established pattern in `services/backupService.test.ts:127-129`). The new block reuses the existing `vi.mock('fs', ...)`, `vi.mock('../services/paths', ...)`, and the existing `vi.mock('../services/backupService', ...)` mocks — extend the `backupService` mock to include the new `latestBackupVersion` export (see "Mock setup" below).

    Mock setup (add to the existing `vi.mock('../services/backupService', ...)` block at the top of the test file; if no such block exists, add one mirroring `services/backupService.test.ts`):

    ```ts
    const latestBackupVersionMock = vi.fn();
    vi.mock('../services/backupService', () => ({
      createVersionedBackup: vi.fn(),
      latestBackupVersion: (...args: unknown[]) => latestBackupVersionMock(...args),
    }));
    ```

    The `diffUtil` module is a pure module — DO NOT mock it. The tests use the real `unifiedDiffText` and `summariseJsonDiff` from `../services/diffUtil` so the end-to-end shape is verified. (If the test file already imports from `../services/diffUtil` indirectly, no change needed.)

    Test cases (all in a single `describe` block, each a focused `it`):

    1. `it('returns 200 with unifiedDiff and summary on the happy path', ...)` — set up `existsSync` so the job dir, `backups/v1/`, and `structured-output.json` all exist; have `readFileSync` return two known pretty-printed JSONs that differ in a single field (e.g. one has `summary: "Original"`, the other has `summary: "Updated"`); call `invokeRoute(router, 'get', '/diffResume?jobDir=shopify-swe&version=v1')`; assert the response status is 200, `body.jobDir` is `'shopify-swe'`, `body.backupVersion` is `1`, `body.unifiedDiff` contains the line `-  "summary": "Original"` (or the equivalent pretty-printed form — be flexible on whitespace; assert the substring `Original` is present and `Updated` is present in either the diff or the summary), and `body.summary.changedPaths` includes `'summary'`.

    2. `it('returns 404 with "Backup not found" when the backup is missing', ...)` — make `existsSync` return false for the backup file but true for the current; assert `status === 404` and `body.error === 'Backup not found'`, `body.jobDir === 'shopify-swe'`, `body.version === 'v1'`. (The route should not even call `latestBackupVersion` if `version` is supplied — the route uses the supplied value directly. The `latestBackupVersion` mock may not be called.)

    3. `it('returns 404 with "Resume not found" when the current is missing', ...)` — make `existsSync` return true for the backup but false for the current; assert `status === 404` and `body.error === 'Resume not found'`.

    4. `it('returns 400 with "jobDir required" when jobDir is missing', ...)` — call `invokeRoute(router, 'get', '/diffResume?version=v1')`; assert `status === 400` and `body.error === 'jobDir required'`.

    5. `it('returns 400 with "invalid version" for v0, v-1, and vabc', ...)` — three sub-cases in one `it` (or three sibling `it`s; both are fine): call with `version=v0`, `version=v-1`, `version=vabc`; assert each returns `status === 400` and `body.error === 'invalid version'`. The version regex is `/^v\d+$/` after `Number` parse; `0` is not a positive integer and is rejected.

    6. `it('rejects path-traversal payloads via safeRealpath', ...)` — pass `jobDir=../etc`; have `realpathSync` return a path outside the jobs root (e.g. `/etc`); assert `status === 400` and `body.error === 'jobDir escapes jobs root'`. (If the implementation returns 500 instead — see Task 2's edge case — accept either as long as it is not 200 and not 404; the test is gated to assert `body.error` is a string that is not `'jobDir required'` and not `'Backup not found'`.)

    7. `it('defaults to latestBackupVersion when version is omitted', ...)` — set `latestBackupVersionMock` to return `3`; call `/diffResume?jobDir=shopify-swe` (no version); assert `latestBackupVersionMock` was called and `body.backupVersion === 3`.

    8. `it('omits summary when format=unified', ...)` — set up the happy path; call with `format=unified`; assert `body.unifiedDiff` is a non-empty string and `body.summary` is `undefined`.

    9. `it('omits unifiedDiff when format=summary', ...)` — set up the happy path; call with `format=summary`; assert `body.summary.changedPaths` is an array and `body.unifiedDiff` is `undefined`.

    10. `it('returns 400 for invalid format', ...)` — call with `format=garbage`; assert `status === 400` and `body.error === 'invalid format'`.

    Mock-fixture helpers (in the `describe` block's local scope):
    - `setupHappyPathFiles()` — sets up `existsSync` and `readFileSync` so both files exist and return two known pretty-printed JSONs.
    - `setupMissingBackup()` — backup file does not exist; current does.
    - `setupMissingCurrent()` — backup file exists; current does not.
    - Reset all mocks in `beforeEach` (the test file already has this pattern; extend it).

    File-level rules:
    - Append the new `describe` block; do not modify any existing test cases.
    - The new block is under 250 lines.
    - All 10 cases use real `http.request` via the `invokeRoute` helper (re-use it; do not reimplement).
    - Do not mock `../services/diffUtil` — the tests are end-to-end for the diff content.
    - When asserting the `unifiedDiff` content, prefer `expect(body.unifiedDiff).toContain('Original')` (substring) over an exact-match — the line prefix (` `, `-`, `+`) and exact whitespace depend on the implementation in `services/diffUtil.ts`.
  </action>
  <verify>
    <automated>cd /home/adf_home_joel/src/copilot/resume-opencode && npx vitest run routes/generate.test.ts -t "diffResume" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `routes/generate.test.ts` contains a new `describe('GET /generate/diffResume', ...)` block with 10 cases.
    - [ ] All 10 cases pass under `npx vitest run routes/generate.test.ts -t "diffResume"`.
    - [ ] No `vi.mock('../services/diffUtil', ...)` is added (the tests use the real module).
    - [ ] The test for `format=unified` asserts `body.summary` is `undefined`; the test for `format=summary` asserts `body.unifiedDiff` is `undefined`; the happy path includes both.
    - [ ] The traversal test asserts a non-200 response and a non-empty `body.error` string.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Full npm test, npm run build, and Code Health safeguard gate
  <read_first>
    - AGENTS.md §1 (mandatory pre-commit Code Health safeguard rule)
    - .planning/codebase/TESTING.md (route-test pattern: hermetic vitest setup, vi.mock for fs/paths, real http.request)
  </read_first>
  <action>
    Run the full test suite, the production build, and the CodeScene pre-commit Code Health safeguard on the three files this plan modified (`services/backupService.ts`, `routes/generate.ts`, `routes/generate.test.ts`). If any check fails, refactor until clean — do not declare done.

    1. `cd /home/adf_home_joel/src/copilot/resume-opencode && npm test` — every test in the repo must pass, including the new `describe('GET /generate/diffResume', ...)` block. Pay attention to the `applySuggestions` describe block (the existing test still passes — this plan does not modify the apply path).
    2. `cd /home/adf_home_joel/src/copilot/resume-opencode && npm run build` — TypeScript compile must succeed.
    3. Run `pre_commit_code_health_safeguard` on the repo path. The tool reports per-file verdicts. If any of the three files modified by this plan shows `degraded`, refactor and re-run. Do not commit until clean.

    This task is the gate. After it passes, the plan is ready for the executor to commit.
  </action>
  <verify>
    <automated>cd /home/adf_home_joel/src/copilot/resume-opencode && npm test 2>&1 | tail -20 && echo "---BUILD---" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `npm test` exits 0; all existing tests pass, and the new `describe('GET /generate/diffResume', ...)` block passes.
    - [ ] `npm run build` exits 0 with no TypeScript errors.
    - [ ] `pre_commit_code_health_safeguard` reports no regression on `services/backupService.ts`, `routes/generate.ts`, and `routes/generate.test.ts`.
  </acceptance_criteria>
</task>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /generate/diffResume | Untrusted query string from the network. The route is intentionally NOT admin-gated per the source plan (admin-gating is reserved for write endpoints). |
| /generate/diffResume → filesystem | Read-only access to `jobs/<slug>/backups/vN/structured-output.json` and `jobs/<slug>/structured-output.json`. Path must be allowlisted. |
| Resume JSON on disk → response | The response echoes the user's own resume. No PII redaction; the user is reading their own data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Tampering (EoP) | GET /generate/diffResume via `jobDir` | medium | mitigate | `safeRealpath` + `pathIsInsideDir(realJobDir, realJobsRoot)` from `routes/generate.ts:609-622` allowlists the path. Task 2 reuses this exact chain; Task 3 covers it with a route test. |
| T-01-02-02 | Information disclosure | Resume JSON echoed in response | low | accept | Per DIFF-02 and the source plan §Background: "No PII handling on the route — the diff is the user's own data, served to themselves." The response is gated to the user's own files; no third-party call. Risk acceptable for a local single-user tool. |
| T-01-02-03 | Denial of service (resource exhaustion) | Large file read in route handler | low | accept | Resume JSONs are bounded at ~30 KB by the model prompt and the on-disk schema. No read amplification. If a 1 GB file were planted under `jobs/`, the read would block the event loop — but `fs.readFileSync` is bounded by the file size and a 1 GB read on a local file is at most a few seconds. Severity stays low because the attacker is local-only. |
| T-01-02-04 | Repudiation | N/A | n/a | accept | The route is read-only; no state mutation. |
| T-01-02-05 | Information disclosure (default-version) | `latestBackupVersion` reveals existence of backups | low | accept | The user is reading their own job folder; the existence of their own backups is not sensitive. |
| T-01-02-SC | Tampering via npm install | n/a | n/a | skip | This plan introduces no new dependencies without evaluation. |

No threat in this plan reaches `high` severity; the security gate (per `security_asvs_level: 1`, `security_block_on: high`) is satisfied by definition.
</threat_model>

<verification>
Overall verification for Plan 2:

1. `cd /home/adf_home_joel/src/copilot/resume-opencode && npm test` — full vitest run is clean.
2. `cd /home/adf_home_joel/src/copilot/resume-opencode && npm run build` — TypeScript build is clean.
3. `pre_commit_code_health_safeguard` on the repo path — no `degraded` verdict on `services/backupService.ts`, `routes/generate.ts`, or `routes/generate.test.ts`.
4. `curl -s "http://localhost:3001/generate/diffResume?jobDir=shopify-swe&version=v1"` (with the server running) returns 200 with the documented JSON shape — manually confirmed by the executor before commit. (The manual smoke is in Plan 3's runbook; the executor can defer to that if a server is not running locally.)
5. `grep -n "latestBackupVersion" services/backupService.ts` shows the new export.
6. `grep -n "router.get('/diffResume'" routes/generate.ts` shows the new route registration.
7. `grep -n "diffUtil\|backupService" routes/generate.ts` shows the new imports for `unifiedDiffText`, `summariseJsonDiff`, and `latestBackupVersion`.
</verification>

<success_criteria>
- `services/backupService.ts` exports `latestBackupVersion(backupsRoot): number | null` as a pure read.
- `routes/generate.ts` registers `GET /diffResume` with the documented validation rules and response shape.
- The route reuses the existing `safeRealpath` + `pathIsInsideDir` allowlist; path-traversal payloads are rejected.
- `routes/generate.test.ts` covers the happy path, both 404s (missing backup, missing current), the three 400s (missing `jobDir`, bad `version`, bad `format`), path-traversal, the `latestBackupVersion` default, and all three `format` variants (10 cases).
- `npm test` and `npm run build` are clean.
- `pre_commit_code_health_safeguard` reports no regression on the three files this plan modified.
- The threat model is recorded in the plan; no threat reaches `high` severity.
</success_criteria>

<notes_and_decisions>
- **Admin auth (2026-07-18):** Per user decision at plan-check time, the route is NOT wrapped in `requireAdminAuth`. This matches the existing `/generate/*` convention (only `/api/config` and `/api/edit` are admin-gated per `server.ts:65,139`). The path allowlist (`safeRealpath` + `pathIsInsideDir` + `ensureJobsRootRealpath`) is the security boundary for this read-only route. Documented in `REQUIREMENTS.md` DIFF-03.
</notes_and_decisions>

<artifacts>
This phase produces the following user-visible symbols (the "artifacts" the executor must land):

- `services/backupService.ts` (edited):
  - `latestBackupVersion(backupsRoot: string): number | null` — new exported helper. Pure read; uses `fs.existsSync` and `fs.readdirSync`. Returns `null` if the directory is missing or contains no `vN` entries.

- `routes/generate.ts` (edited):
  - New imports: `unifiedDiffText`, `summariseJsonDiff` from `../services/diffUtil`; `latestBackupVersion` from `../services/backupService`.
  - `validateDiffResumeRequest(req)` — new validation helper using the existing `ValidationFailure | ValidationSuccess<T>` discriminated union. Returns `400 { error: 'jobDir required' }` for missing `jobDir`, `400 { error: 'jobDir escapes jobs root' }` for traversal, `400 { error: 'invalid version' }` for bad versions, `400 { error: 'invalid format' }` for bad format, `404 { error: 'Backup not found', jobDir, version }` for missing backup, `404 { error: 'Resume not found', jobDir, version }` for missing current, or `200` with the response object.
  - `diffResumeHandler(req, res)` — new route handler. Reads both files, pretty-prints via `JSON.stringify(_, null, 2)`, computes `unifiedDiff` and `summary` from `diffUtil`, and assembles the response per the format flag.
  - `router.get('/diffResume', diffResumeHandler)` — new route registration. Mounted under the existing `/generate` sub-router; no `requireAdminAuth` wrap (intentional, per the source plan §Step 2.1 note).

- `routes/generate.test.ts` (edited):
  - Mock extension: add `latestBackupVersion` to the existing `vi.mock('../services/backupService', ...)` block.
  - New `describe('GET /generate/diffResume', ...)` block with 10 cases: happy path; missing backup 404; missing current 404; missing `jobDir` 400; bad `version` 400 (v0/v-1/vabc); path-traversal 400; default to `latestBackupVersion`; `format=unified` omits `summary`; `format=summary` omits `unifiedDiff`; invalid `format` 400.
</artifacts>

<output>
Create `.planning/phases/01-resume-json-diff-viewer/01-02-SUMMARY.md` when done.
</output>




