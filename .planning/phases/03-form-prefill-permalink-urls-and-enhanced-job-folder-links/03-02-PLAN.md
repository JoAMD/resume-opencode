---
phase: "03"
plan: "02"
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - routes/generate.ts
  - routes/generate.test.ts
  - public/index.html
  - public/suggestions.js
  - public/style.css
  - .planning/phases/03-form-prefill-permalink-urls-and-enhanced-job-folder-links/03-02-SUMMARY.md
autonomous: true
requirements:
  - STATUS-01

must_haves:
  truths:
  - When a user loads a permalink for a job folder that has an in-progress task, a 4-step indicator appears with descriptive labels (Generating resume + cover letter, Running ATS analysis, Applying ATS suggestions, Final ATS analysis)
  - The status indicator updates as the polled task progresses; the step is derived from the taskId in `sessionStorage['taskId_<jobDir>']` and the polled response shape
  - If the taskId is not in sessionStorage but the job folder has artifact files (resume.pdf, cover-letter.pdf, ats-analysis.md), the result block is shown without the status panel
  - The existing `/generate/task/:taskId` polling endpoint is reused — no new endpoint is created
  - Each artifact in the result block shows a spinner next to its name while pending, and a normal link once the file exists
  - The fire-and-forget auto-chain (generate → applySuggestions → runAtsBackground) stores the `taskId` in `sessionStorage['taskId_<jobDir>']` so a refresh during the chain reattaches to the same task

---

<objective>
Show the user what's happening when they load a permalink to a job folder while generation is still in flight. A 4-step indicator with descriptive labels replaces the raw spinner, inline spinners appear next to each pending artifact, and the existing `GET /generate/task/:taskId` polling endpoint is reused (no new endpoint). The fire-and-forget auto-chain stores each taskId under `sessionStorage['taskId_<jobDir>']` so a refresh during the chain reattaches automatically.
</objective>

<execution_context>
@/home/adf_home_joel/src/copilot/resume-opencode/.planning/phases/03-form-prefill-permalink-urls-and-enhanced-job-folder-links/03-CONTEXT.md
</execution_context>

<context>
@public/index.html §"generateResume" (lines 466-596) — the fire-and-forget auto-chain (generate → applySuggestions → runAtsBackground); this is where the taskId sessionStorage writes need to land
@public/index.html §"generateResume" success path — `lastTaskId = data.taskId;` on line 558 is the right hook point for the sessionStorage write
@public/index.html §"pollApplySuggestionsThenAts" (lines 391-417) — currently fires-and-forgets the ATS background task; the apply-suggestions taskId is `data.taskId` on line 539 of suggestions.js
@public/suggestions.js §"applyBtn click handler" (lines 524-555) — the explicit `Apply suggestions` button click; the same taskId sessionStorage write is needed here
@public/suggestions.js §"waitForTask" (lines 412-425) — existing polling helper
@public/suggestions.js §"open-diff-for-latest event" — Plan 01 dispatches this from the result block; Plan 02 consumes it
@routes/generate.ts §"taskMap" (line 67) — `Map<string, TaskResult & { startedAt: number }>`; the per-task `result.coverLetterSessionId` is already there. The new `step` field is added to the task result map.
@routes/generate.ts §"GET /generate/task/:taskId" (lines 227-242) — the polling endpoint; expose `step` in the response
@routes/generate.ts §"POST /generate/" IIFE (lines 302-311) — set the initial step to 1 (combined generation) or 1 (resume-only); the cover-letter step is set by the cover-letter IIFE if needed
@routes/generate.ts §"runApplySuggestionsBackground" (lines 968-1001) — set the step to 3 (apply ATS suggestions) when the task starts; set to 4 (final ATS analysis) when the ATS background completes
@routes/generate.ts §"runAtsBackground" (lines 1041-1097) — this is the "Final ATS analysis" step; the step was set to 3 by the apply task and should become 4 once this task completes
@public/style.css §".inline-spinner" (added in Plan 01) — reused for the pending-artifact spinners
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server — extend taskMap to track current step; expose step in the polling response</name>
  <files>routes/generate.ts, routes/generate.test.ts</files>
  <read_first>
@routes/generate.ts §"taskMap" declaration (line 67) — `const taskMap = new Map<string, TaskResult & { startedAt: number }>();` — the per-task record stores the result and the startedAt; the new `step` field is added to this record
@routes/generate.ts §"GET /generate/task/:taskId" handler (lines 227-242) — the polling endpoint; expose `step` in the JSON response
@routes/generate.ts §"POST /generate/" IIFE (lines 302-311) — the combined/resume-only background task; set the initial step to 1 ("Generating resume + cover letter") and bump to 2 ("Running ATS analysis") when the atsKeywords path completes inside `executeGeneration`
@routes/generate.ts §"executeGeneration" (lines 1179-1271) — the long-running function called by the IIFE; the atsKeywords run is around line 1241-1268
@routes/generate.ts §"POST /generate/coverLetter" (lines 319-365) — the cover-only path; runs synchronously; not affected by the status panel (cover letter is fast, no background task)
@routes/generate.ts §"runApplySuggestionsBackground" (lines 968-1001) — the apply-suggestions background task; set its step to 3 ("Applying ATS suggestions") when the task starts
@routes/generate.ts §"runAtsBackground" (lines 1041-1097) — the "Final ATS analysis" task; the apply task is responsible for setting its taskId to step 3, this task bumps to 4 ("Final ATS analysis") when the task starts
@routes/generate.ts §"taskMap.set" call sites (lines 299, 305, 309, 1022, 979, 988, 998, 1086, 1094, 1110) — every place where the task record is updated; the new `step` field is added at each of these
@routes/generate.test.ts §"describe('GET /generate/diffResume')" (line 511) — reference pattern for a route test that asserts the response body shape
  </read_first>
  <action>
In `routes/generate.ts`:

1. Extend the `TaskResult` type (line 59-65) with an optional `step: number` field (1..4 per D-25) and an optional `stepLabel: string` (the human-readable label). The step number is the source of truth; the label is computed from the step number using a small constant table in the client (and the server exposes it for the rare case where the client is older than the server). Default both to undefined when not set; the client treats undefined as "step 1".

2. Extend the `taskMap` record type at line 67 to `Map<string, TaskResult & { startedAt: number; step: number; stepLabel: string }>`. The `step` and `stepLabel` are always present once a task is created.

3. Add a small `STEP_LABELS: Record<number, string>` constant near the top of the file:
   ```ts
   const STEP_LABELS: Record<number, string> = {
     1: 'Generating resume + cover letter',
     2: 'Running ATS analysis',
     3: 'Applying ATS suggestions',
     4: 'Final ATS analysis',
   };
   ```
   Plus a `currentStep(taskId, step)` helper that updates the taskMap record: `taskMap.get(taskId)!.step = step; taskMap.get(taskId)!.stepLabel = STEP_LABELS[step] ?? '';`.

4. Update the `POST /generate/` IIFE (line 299-311):
   - At task creation (`taskMap.set` at line 299), set `step: 1, stepLabel: STEP_LABELS[1]`.
   - POST /generate/ task stays at step 1 throughout. The status panel transitions through steps 1 → 3 → 4 as separate taskIds complete (no in-process step-2 bump).
   - Do NOT call `setTaskStep` from inside `executeGeneration`. `executeGeneration`'s signature remains `(jobDir, options, input)` and does not receive `taskId`.

5. Update `runApplySuggestionsBackground` (line 968-1001) to set `step: 3, stepLabel: STEP_LABELS[3]` at all three `taskMap.set` call sites in this function:
   - line 979 (success path): `step: 3, stepLabel: STEP_LABELS[3]`.
   - line 988 (no-op path): `step: 3, stepLabel: STEP_LABELS[3]`.
   - line 998 (error path): `step: 3, stepLabel: STEP_LABELS[3]`.
   The step stays at 3 for the duration of the apply-suggestions task; the next phase (Final ATS analysis) is a separate task at step 4.

6. Update `runAtsBackground` (line 1041-1097) to set `step: 4, stepLabel: STEP_LABELS[4]` at both `taskMap.set` call sites in this function:
   - line 1086 (success path): `step: 4, stepLabel: STEP_LABELS[4]`.
   - line 1094 (error path): `step: 4, stepLabel: STEP_LABELS[4]`.

7. Update the `GET /generate/task/:taskId` handler (line 227) to include `step` and `stepLabel` in the response body:
   ```ts
   res.json({
     status: task.status,
     result: task.result,
     error: task.error,
     startedAt: task.startedAt,
     sessionId: task.sessionId,
     coverLetterSessionId: task.coverLetterSessionId,
     step: task.step,
     stepLabel: task.stepLabel,
   });
   ```

8. Add a helper `setTaskStep(taskId: string, step: number): void` near `currentStep` that does the same thing with a cleaner name. Replace the inline `currentStep` calls with `setTaskStep` for readability. Export `setTaskStep` and `STEP_LABELS` from the module so tests can verify them.

9. Set `step` and `stepLabel` at every `taskMap.set` call site. Required by the `TaskResult` type extension in action #2. Use `STEP_LABELS[N]` to derive the label. The full list of sites:

   | Line | Route/function | Status | step | stepLabel |
   |------|----------------|--------|------|-----------|
   | 299  | `POST /generate/` (pending)             | `pending`  | 1 | `STEP_LABELS[1]` |
   | 305  | `POST /generate/` (complete)             | `complete` | 1 | `STEP_LABELS[1]` |
   | 309  | `POST /generate/` (error)                | `error`    | 1 | `STEP_LABELS[1]` |
   | 979  | `runApplySuggestionsBackground` (success)| `complete` | 3 | `STEP_LABELS[3]` |
   | 988  | `runApplySuggestionsBackground` (no-op)  | `error`    | 3 | `STEP_LABELS[3]` |
   | 998  | `runApplySuggestionsBackground` (error)  | `error`    | 3 | `STEP_LABELS[3]` |
   | 1022 | `POST /generate/applySuggestions` (pending) | `pending`  | 3 | `STEP_LABELS[3]` |
   | 1086 | `runAtsBackground` (success)             | `complete` | 4 | `STEP_LABELS[4]` |
   | 1094 | `runAtsBackground` (error)               | `error`    | 4 | `STEP_LABELS[4]` |
   | 1110 | `POST /generate/runAtsBackground` (pending) | `pending`  | 4 | `STEP_LABELS[4]` |

   Note: the `POST /generate/coverLetter` route (line 319-365) does NOT use `taskMap` and is not affected. The cover letter is a synchronous response, not a background task.

10. Add tests in `routes/generate.test.ts`:
    - Add a `describe('Task step tracking', ...)` block that exercises the polling endpoint and asserts:
      - When a `POST /generate/` task is created, the task record has `step === 1` and `stepLabel === 'Generating resume + cover letter'`.
      - `GET /generate/task/:taskId` returns `step: 1, stepLabel: 'Generating resume + cover letter'` in the response body.
      - `setTaskStep(taskId, 3)` mutates the taskMap and the next `GET /generate/task/:taskId` returns `step: 3, stepLabel: 'Applying ATS suggestions'`.
    - Mock the AI to resolve quickly so the test can `await invokeRoute(...)` once, capture the taskId from the response, then `setTaskStep(taskId, N)` and assert via a second `GET /generate/task/:taskId`.
    - Reuse the existing `vi.mock('../services/ai', ...)` factory (line 69) — make `generateCombinedJSON` resolve with `{ resume: { name: 'X', skills: {}, experience: [], education: [], projects: [] }, coverLetter: { ... }, sessionId: 'sess-1', coverLetterSessionId: 'sess-2', atsKeywords: ['kw1', 'kw2'] }`.
  </action>
  <verify>
<automated>npx vitest run routes/generate.test.ts 2>&1 | tail -20</automated>
</verify>
  <done>
- `GET /generate/task/:taskId` response includes `step: number` and `stepLabel: string`
- A new `POST /generate/` task has `step: 1, stepLabel: 'Generating resume + cover letter'`
- An apply-suggestions task has `step: 3, stepLabel: 'Applying ATS suggestions'`
- An ATS background task has `step: 4, stepLabel: 'Final ATS analysis'`
- `setTaskStep(taskId, n)` mutates the taskMap and the next poll returns the new step
- `STEP_LABELS` is exported and tested
  </done>
</task>

<task type="auto">
  <name>Task 2: Client — status panel UI, taskId sessionStorage writes, inline spinners</name>
  <files>public/index.html, public/suggestions.js, public/style.css</files>
  <read_first>
@public/index.html §"generateResume" success path (lines 536-580) — `lastTaskId = data.taskId;` on line 558 is where the sessionStorage write for the initial taskId lands; the auto-apply `fetch('/generate/applySuggestions')` on line 564 needs a similar write for the apply-suggestions taskId
@public/index.html §"pollApplySuggestionsThenAts" (lines 391-417) — currently fires-and-forgets the ATS background; the `atsData.taskId` on line 406 needs a sessionStorage write for the ATS background taskId
@public/index.html §"#status" element (line 143) — current "status" element; the new status panel is a separate element so the existing one can keep its narrow role (form status, "Generating resume…" message)
@public/suggestions.js §"applyBtn click handler" (lines 524-555) — the explicit `Apply suggestions` button click; the same taskId sessionStorage write is needed here
@public/suggestions.js §"waitForTask" (lines 412-425) — existing polling helper; the new status panel reuses the same fetch URL and shape
@public/suggestions.js §"open-diff-for-latest event listener" — Plan 02 adds this listener to consume Plan 01's event
@public/style.css §".inline-spinner" (added in Plan 01) — reused for the pending-artifact spinners
@public/style.css §".result" (lines 180-204) — existing result block styles; the new status panel sits below the result block when active
  </read_first>
  <action>
In `public/index.html`:

1. Add a new status panel just after the existing `<div id="status">` (line 143) and `<div id="ats-toast">` (line 144), and before the JD search panel:
   ```html
   <section id="status-panel" class="status-panel hidden" aria-live="polite">
     <h3>Generation in progress</h3>
     <ol class="status-steps">
       <li class="status-step" data-step="1"><span class="inline-spinner"></span> <span class="status-step-label">Generating resume + cover letter</span></li>
       <li class="status-step" data-step="2"><span class="inline-spinner"></span> <span class="status-step-label">Running ATS analysis</span></li>
       <li class="status-step" data-step="3"><span class="inline-spinner"></span> <span class="status-step-label">Applying ATS suggestions</span></li>
       <li class="status-step" data-step="4"><span class="inline-spinner"></span> <span class="status-step-label">Final ATS analysis</span></li>
     </ol>
   </section>
   ```
   The `<span class="inline-spinner">` is invisible (CSS `display: none`) until the step is active; the spinner appears only for the current step. Use a `data-active="true"` attribute on the active step to toggle the spinner.

2. Add the following functions inside the existing inline `<script>` block in `public/index.html`, near the `runPrefill` function (added in Plan 01):

   - `function taskIdKey(slug) { return 'taskId_' + slug; }` and `function taskChainKey(slug) { return 'taskChain_' + slug; }` — sessionStorage keys per D-23.

   - `function recordTaskId(slug, taskId)` — `sessionStorage.setItem(taskIdKey(slug), taskId);` AND `sessionStorage.setItem(taskChainKey(slug), JSON.stringify([...existingChain, taskId]));`. The chain is a JSON array of the last 4 taskIds for this slug (D-25 has 4 steps).

   - `function loadTaskChain(slug)` — returns `JSON.parse(sessionStorage.getItem(taskChainKey(slug)) || '[]')` and `sessionStorage.getItem(taskIdKey(slug))` (the most recent).

   - `function clearTaskChain(slug)` — `sessionStorage.removeItem(taskIdKey(slug)); sessionStorage.removeItem(taskChainKey(slug));`.

   - `async function pollChain(slug, taskIds)` — for each taskId, `fetch('/generate/task/' + taskId)` and return the union of step numbers (max wins). Stop when the max step is 4 AND all tasks are complete. **Caveat:** the server's taskMap is in-memory and resets on server restart, so a refresh after a restart will get 404 from the polling endpoint. In that case, show a "Server restarted — chain lost" toast and call `clearTaskChain(slug)`. The status panel closes.

   - `function renderStatusPanel(maxStep, taskResults)` — toggle the active class on each `<li class="status-step" data-step="N">`. Show the spinner next to the current step and the steps that haven't been completed yet. Hide the panel when maxStep >= 4 AND all tasks are complete (after a 1s pause so the user sees the "Final ATS analysis" tick); the result block then takes over.

   - `async function startStatusPolling(slug)` — if `loadTaskChain(slug)` is non-empty, show the status panel and call `pollChain` in a loop with a 2s interval. When the chain is done, clear it and hide the panel. Called from `runPrefill` (added in Plan 01) at the end of the success path.

3. Wire the taskId sessionStorage writes from three places:
   - In `generateResume` (line 558 `lastTaskId = data.taskId;`), add `recordTaskId(result.jobDir, data.taskId);` immediately after.
   - In the auto-apply `fetch('/generate/applySuggestions').then(...)` block (lines 575-579), add `recordTaskId(result.jobDir, data.taskId);` inside the `.then((data) => { if (data.taskId) { recordTaskId(...); pollApplySuggestionsThenAts(...); } })` body.
   - In `pollApplySuggestionsThenAts` (line 400 `fetch('/generate/runAtsBackground')`, inside the `.then` callback at lines 400-408), add `recordTaskId(jobDir, atsData.taskId);` after `if (atsData.taskId) { ... }`.

4. In `runPrefill` (added in Plan 01), after the result block is shown, call `startStatusPolling(slug)` (added in this task). If the chain is empty, this is a no-op and the result block stays visible without a status panel (D-26).

5. Wire the `open-diff-for-latest` event listener. Inside the existing inline `<script>` block in `public/index.html`, near the bottom (after the `runPrefill` definition), add:
   ```js
   window.addEventListener('open-diff-for-latest', async (e) => {
     const slug = e.detail && e.detail.slug;
     if (!slug) return;
     try {
       const res = await fetch('/generate/listBackups?jobDir=' + encodeURIComponent(slug));
       const data = await res.json();
       const versions = (data.versions || []);
       if (!versions.length) {
         // Use the existing #ats-toast element for non-blocking errors per codebase convention.
         const toast = document.getElementById('ats-toast');
         if (toast) {
           toast.textContent = 'No backup versions found for this job folder.';
           toast.classList.remove('hidden');
           setTimeout(() => toast.classList.add('hidden'), 6000);
         }
         return;
       }
       const latest = Math.max(...versions);
       if (typeof globalThis.openDiffModal === 'function') {
         globalThis.openDiffModal(latest, null, slug);
       } else {
         // suggestions.js hasn't loaded yet — poll briefly
         let waited = 0;
         while (typeof globalThis.openDiffModal !== 'function' && waited < 2000) {
           await new Promise(r => setTimeout(r, 50));
           waited += 50;
         }
         if (typeof globalThis.openDiffModal === 'function') {
           globalThis.openDiffModal(latest, null, slug);
         } else {
           // Final fallback: surface the error via the existing #ats-toast.
           const toast = document.getElementById('ats-toast');
           if (toast) {
             toast.textContent = 'Diff modal not yet loaded. Try again in a moment.';
             toast.classList.remove('hidden');
             setTimeout(() => toast.classList.add('hidden'), 6000);
           }
         }
       }
     } catch (err) {
       const toast = document.getElementById('ats-toast');
       if (toast) {
         toast.textContent = 'Failed to open diff: ' + (err && err.message ? err.message : err);
         toast.classList.remove('hidden');
         setTimeout(() => toast.classList.add('hidden'), 6000);
       }
     }
   });
   ```

   Also add a listener for the `apply-task-started` event (defined in `public/suggestions.js` action #7 below). The listener uses the `slug` from the event detail (NOT `getJobSlug`, which is scoped to the `suggestions.js` IIFE):
   ```js
   window.addEventListener('apply-task-started', (e) => {
     const { taskId, slug } = (e && e.detail) || {};
     if (!taskId || !slug) return;
     recordTaskId(slug, taskId);
   });
   ```

In `public/suggestions.js`:

6. Expose `openDiffModal` on `globalThis` so the inline script in `public/index.html` can call it. Near the end of the file (after the `if (document.readyState === 'loading')` block at line 678), add:
   ```js
   globalThis.openDiffModal = openDiffModal;
   ```

7. In the `applyBtn` click handler (line 539 `const { taskId } = await postApplySuggestions(...)`), after destructuring `taskId`, dispatch a `CustomEvent('apply-task-started', { detail: { taskId, slug } })` so the inline script in `public/index.html` can record it. The `slug` is included in the event detail because `getJobSlug` is scoped to the `initSuggestionsPanel` IIFE in `public/suggestions.js:58-60` and is not accessible from the inline script in `public/index.html`. **Event detail contract (locked):** `{ taskId: string, slug: string }`. The inline script listener reads both fields via `e.detail.taskId` and `e.detail.slug`, then calls `recordTaskId(slug, taskId)`. **Why an event and not a direct call?** suggestions.js is a module loaded after the inline script; the inline script registers the listener on `window` early, then suggestions.js dispatches the event. The reverse direction (suggestions.js calling into the inline script) requires the global registry approach. This matches the `auto-apply-complete` event pattern that already exists (line 568).

In `public/style.css`:

8. Add the following styles:
   - `.status-panel { background: #1a1a2e; border-radius: 6px; padding: 16px; margin-top: 16px; }` and `.status-panel.hidden { display: none; }` and `.status-panel h3 { margin: 0 0 8px 0; font-size: 1em; }`.
   - `.status-steps { list-style: none; padding: 0; margin: 0; }` and `.status-step { display: flex; align-items: center; gap: 8px; padding: 4px 0; color: #888; }` — every step is dimmed by default.
   - `.status-step[data-active="true"] { color: #fff; }` and `.status-step[data-active="true"] .inline-spinner { display: inline-block; }` — only the active step shows the spinner.
   - `.status-step[data-done="true"] { color: #86efac; }` and `.status-step[data-done="true"]::before { content: '✓'; color: #86efac; margin-right: 4px; }` — completed steps show a checkmark.
   - `.status-step .inline-spinner { display: none; }` — default hidden.
  </action>
  <verify>
<automated>grep -c "status-panel\|status-step\|recordTaskId\|loadTaskChain\|open-diff-for-latest" public/index.html && grep -c "globalThis.openDiffModal" public/suggestions.js && grep -c "status-panel\|status-step" public/style.css</automated>
</verify>
  <done>
- The `<section id="status-panel">` with 4 `<li class="status-step">` rows is present in `public/index.html`; only the active step shows a spinner and the completed steps show a checkmark
- `recordTaskId` / `loadTaskChain` / `clearTaskChain` functions are defined; they write/read `taskId_<slug>` and `taskChain_<slug>` in `sessionStorage`
- The three sessionStorage writes are wired: at the start of the generation task, at the start of the apply-suggestions task, and at the start of the ATS background task
- `startStatusPolling(slug)` is called from `runPrefill` (Plan 01) when the chain is non-empty; the status panel polls the active taskIds and renders the max step across them
- If a poll returns 404 (server restarted, in-memory taskMap lost), the chain is cleared and a toast appears
- `globalThis.openDiffModal` is exposed from `suggestions.js` and consumed by the inline-script listener for the `open-diff-for-latest` event
- The "Compare with latest backup" button (Plan 01) opens the diff modal via the event listener
  </done>
</task>

<task type="auto">
  <name>Task 3: Documentation + manual runbook + Code Health safeguard</name>
  <files>README.md, docs/FEATURES.md, .planning/phases/03-form-prefill-permalink-urls-and-enhanced-job-folder-links/03-02-SUMMARY.md</files>
  <read_first>
@README.md §"Apply suggestions" — extend with a "Generation status on permalink load" paragraph
@docs/FEATURES.md §"Generate resume" — extend with a "Generation status" bullet
  </read_first>
  <action>
1. In `README.md`, add a new paragraph to the "Permalinks and form prefill" section (added in Plan 01) titled "Generation status". One paragraph covering:
   - When a permalink is loaded for a job folder where generation is still in flight, a 4-step indicator shows the current step
   - The chain's taskIds are recorded in `sessionStorage` keyed by slug, so a refresh reattaches to the same chain
   - The existing `GET /generate/task/:taskId` polling endpoint is reused

2. In `docs/FEATURES.md`, add a bullet to the "Generate resume" section:
   - Bullet: "Generation status: While the auto-chain (generate → apply suggestions → ATS analysis) runs, each taskId is recorded in `sessionStorage['taskId_<slug>']`. Loading the permalink for a folder with an active chain shows a 4-step status indicator with descriptive labels and inline spinners next to pending artifacts. When the chain completes, the status panel closes and the result block is shown."

3. In `.planning/phases/03-form-prefill-permalink-urls-and-enhanced-job-folder-links/`, create `03-02-SUMMARY.md` summarising what shipped in Plan 02: the new `step`/`stepLabel` fields in the taskMap and the polling response, the status panel UI with 4 steps, the `taskId_<slug>` and `taskChain_<slug>` sessionStorage writes, the inline artifact spinners, the `open-diff-for-latest` event listener wired to `openDiffModal`, and the test cases added. Reference the test cases that pass and note the manual runbook was executed.

4. Run `npm test` — must be clean. Run `npm run build` — must be clean.

5. Run `pre_commit_code_health_safeguard` on the changes. If any file reports a regression, refactor before declaring the plan done (per the AGENTS.md safeguard rule).

6. Execute the manual runbook (record the steps and outcomes in the SUMMARY):
   - Start a generation; mid-generation, copy the permalink URL, open a new tab, paste it. Confirm the status panel shows step 1 with a spinner. Refresh the tab; confirm the chain continues from the same taskId.
   - Wait for the auto-chain to fire (apply suggestions + ATS); confirm the status panel advances through steps 3 and 4 with descriptive labels and inline spinners next to the pending artifacts. When the chain completes, the status panel closes.
   - Reload the permalink after the chain completes; confirm the status panel does NOT appear (D-26) and the result block is shown instead.
   - Click "Compare with latest backup" from the result block; confirm the diff modal opens.
   - Stop the server mid-generation; reload the permalink; confirm the "Server restarted — chain lost" toast appears and the status panel closes.
  </action>
  <verify>
<automated>npm test 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
</verify>
  <done>
- `README.md` has a "Generation status" paragraph documenting the 4-step indicator, the sessionStorage keys, and the reuse of the existing polling endpoint
- `docs/FEATURES.md` has a new "Generation status" bullet under "Generate resume"
- `03-02-SUMMARY.md` exists, summarises what shipped, and records the manual runbook outcomes
- `npm test` is clean
- `npm run build` is clean
- `pre_commit_code_health_safeguard` reports no regression
- The manual runbook was executed and the outcomes recorded in the SUMMARY
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server `/generate/task/:taskId` | The user-supplied slug determines which taskId is in sessionStorage; the server's taskMap is keyed by an opaque taskId. No new trust boundary is introduced; the polling endpoint is read-only and the same allow-list applies. |
| Browser → sessionStorage `taskId_<slug>` | The user can manipulate this value. The server treats unknown taskIds as 404 and the client falls back to the empty chain. No escalation possible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Information Disclosure | Status panel polls a taskId that belongs to a different user (theoretical; the tool is single-user) | low | accept | Single-user local tool; no auth boundary; the existing `/generate/task/:taskId` is read-only and returns minimal status info. |
| T-03-10 | Tampering | User manipulates `sessionStorage['taskId_<slug>']` to point at an arbitrary taskId | low | accept | The polling endpoint returns 404 for unknown taskIds; the client falls back to the empty chain. No write paths are exposed. |
| T-03-11 | Denial of Service | Status panel polls a long-running taskId indefinitely | low | mitigate | The polling loop stops when the chain is complete (max step + all tasks complete). The "Server restarted" path clears the chain on 404. The chain is capped at 4 taskIds (D-25). |
| T-03-12 | Spoofing | User opens a permalink with `#job=../etc` | low | mitigate | Plan 01's prefill route already rejects path-traversal with 400. The status panel's `loadTaskChain` reads from `sessionStorage`; if the slug is malicious, the chain lookup returns `[]` and the panel is hidden. |
| T-03-13 | Tampering | npm/pip/cargo installs | high | mitigate | Phase 3 does not introduce new dependencies. The `pre_commit_code_health_safeguard` is the existing gate. |
</threat_model>

<verification>
1. `npx vitest run routes/generate.test.ts` — passes (including the new step-tracking tests)
2. `npm test` — all tests pass, no regressions
3. `npm run build` — clean
4. `pre_commit_code_health_safeguard` — no regression
5. Manual runbook steps 1-5 executed and recorded in the SUMMARY
6. The status panel reuses the existing `/generate/task/:taskId` polling endpoint — no new endpoint is created (D-27)
</verification>

<success_criteria>
- The `taskMap` record stores `step` and `stepLabel`; the polling endpoint returns both
- A new `POST /generate/` task starts at step 1; the apply-suggestions task at step 3; the ATS background task at step 4
- The status panel renders 4 steps with descriptive labels and an inline spinner next to the active step
- The chain's taskIds are stored in `sessionStorage['taskId_<slug>']` and `taskChain_<slug>`; a refresh reattaches to the same chain
- If the taskId is not in sessionStorage but the job folder has artifacts, the result block is shown without the status panel (D-26)
- If the server restarts mid-chain, the status panel detects 404 and clears the chain
- The "Compare with latest backup" button (Plan 01) opens the diff modal via the `open-diff-for-latest` event
- README.md and docs/FEATURES.md are updated; the manual runbook is executed; Code Health safeguard is clean
</success_criteria>

<output>
Create `.planning/phases/03-form-prefill-permalink-urls-and-enhanced-job-folder-links/03-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

### Server (`routes/generate.ts`)

**New constants and exports:**
- `STEP_LABELS: Record<number, string>` — `{ 1: 'Generating resume + cover letter', 2: 'Running ATS analysis', 3: 'Applying ATS suggestions', 4: 'Final ATS analysis' }`.
- `setTaskStep(taskId: string, step: number): void` — mutates the taskMap record's `step` and `stepLabel` fields.

**Modified types:**
- `TaskResult` (line 59-65) — adds optional `step: number` and `stepLabel: string` (the `taskMap` record type at line 67 makes both required after creation).
- The polling endpoint response at `GET /generate/task/:taskId` now includes `step` and `stepLabel`.

**Step assignments (where `taskMap.set` is called):**
- `POST /generate/` IIFE (line 299 / 305 / 309) — initial and completion `step: 1` ("Generating resume + cover letter"). The post-generation ATS run inside `executeGeneration` (line 1241-1268) bumps the same task to `step: 2` ("Running ATS analysis") via `setTaskStep(taskId, 2)`.
- `runApplySuggestionsBackground` (line 979 / 988 / 998) — `step: 3` ("Applying ATS suggestions") at all three sites.
- `POST /generate/applySuggestions` route (line 1022) — `step: 3` at task creation.
- `runAtsBackground` (line 1086 / 1094) — `step: 4` ("Final ATS analysis") at both sites.
- `POST /generate/runAtsBackground` route (line 1110) — `step: 4` at task creation.

### Server tests (`routes/generate.test.ts`)

- `describe('Task step tracking', ...)` — asserts initial step on `POST /generate/`, that `GET /generate/task/:taskId` returns `step` and `stepLabel`, and that `setTaskStep(taskId, n)` mutates the response.

### Client (`public/index.html`)

**New DOM elements:**
- `<section id="status-panel">` containing `<ol class="status-steps">` with four `<li class="status-step" data-step="N">` rows, each with a `<span class="inline-spinner">` and a `<span class="status-step-label">`.

**New JavaScript functions (inline script):**
- `taskIdKey(slug: string): string` — `'taskId_' + slug`.
- `taskChainKey(slug: string): string` — `'taskChain_' + slug`.
- `recordTaskId(slug: string, taskId: string): void` — writes `taskId_<slug>` and appends to the JSON array in `taskChain_<slug>` (capped at 4 entries).
- `loadTaskChain(slug: string): string[]` — reads and parses the chain.
- `clearTaskChain(slug: string): void` — removes both keys.
- `pollChain(slug: string, taskIds: string[]): Promise<{ maxStep, results }>` — polls each taskId via `GET /generate/task/:taskId` and returns the max step across results; on 404 the chain is cleared and a toast appears.
- `renderStatusPanel(maxStep: number, taskResults): void` — toggles `data-active` and `data-done` on the four `<li>` rows.
- `startStatusPolling(slug: string): Promise<void>` — entry point called from `runPrefill` (Plan 01); 2s poll loop until the chain is complete, then hides the panel.
- `window.addEventListener('open-diff-for-latest', async (e) => { ... })` — handler that calls `globalThis.openDiffModal(latest, null, slug)`, fetched via `GET /generate/listBackups?jobDir=<slug>`.

**New sessionStorage writes (3 call sites):**
- `runPrefill` / `generateResume` success path: `recordTaskId(result.jobDir, data.taskId)` (line 558 in the current file).
- Auto-apply `fetch('/generate/applySuggestions')` success: `recordTaskId(result.jobDir, data.taskId)`.
- `pollApplySuggestionsThenAts` / `fetch('/generate/runAtsBackground')` success: `recordTaskId(jobDir, atsData.taskId)`.

### Client (`public/suggestions.js`)

**New exports:**
- `globalThis.openDiffModal = openDiffModal;` — exposes the existing `openDiffModal` function (Phase 1/2) on the global object so the inline script in `public/index.html` can call it after a small retry loop.

**New event dispatches:**
- In the `applyBtn` click handler: `window.dispatchEvent(new CustomEvent('apply-task-started', { detail: { taskId, slug } }))` after the apply-suggestions `taskId` is received. The inline script listens for this event and calls `recordTaskId(slug, taskId)`.

### Styles (added to `public/style.css`)

- `.status-panel` / `.status-panel.hidden` — the section background and toggle.
- `.status-panel h3` — the heading.
- `.status-steps` / `.status-step` — the four rows; default state is dimmed grey.
- `.status-step[data-active="true"]` — the current step is bright white and shows the spinner.
- `.status-step[data-active="true"] .inline-spinner` — the spinner becomes visible.
- `.status-step[data-done="true"]` — completed steps are green and show a checkmark via `::before`.
- `.status-step .inline-spinner` — default `display: none`.

### Storage keys (new)

- `sessionStorage['taskId_<slug>']` — the most recent active taskId for a job folder. D-23.
- `sessionStorage['taskChain_<slug>']` — JSON array of the last 4 taskIds for a job folder.
