---
phase: "03"
plan: "03"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - public/index.html
  - public/suggestions.js
  - public/style.css
  - docs/FEATURES.md
autonomous: true
requirements: []

must_haves:
  truths:
    - The page loads without logging `ReferenceError: openDiffModal is not defined` in the browser console
    - Clicking "Compare with latest backup" from the result block opens the diff modal and does NOT show the "Diff modal not yet loaded" toast
    - Clicking the permalink Copy button writes the permalink URL to the clipboard and shows "Copied!" for 1.2s on a non-secure-context HTTP origin (e.g. http://192.168.x.x:3000)
    - Clicking any of the four artifact Copy buttons (resume.pdf, cover-letter.pdf, cover-letter.txt, ats-analysis.md) writes the absolute URL to the clipboard and shows "Copied!" for 1.2s
    - Clicking the existing Folder Path Copy button (`#copy-path-btn`) and the existing OpenCode session-id Copy button (`#session-id-copy`) also work on a non-secure-context HTTP origin
    - The permalink URL in the result block visually reads as a clean anchor link (monospace code-style text), not a button-driven control
  artifacts:
    - `public/index.html` defines a single `copyToClipboard(text: string): Promise<boolean>` helper near the top of the inline `<script>` block, before the four call sites that previously called `navigator.clipboard.writeText` directly
    - `public/suggestions.js` declares `globalThis.openDiffModal = openDiffModal;` INSIDE the `initSuggestionsPanel` IIFE (immediately after the `openDiffModal` function declaration ends around the existing line 341), so the identifier is in scope
    - `public/suggestions.js` does NOT contain `globalThis.openDiffModal = openDiffModal;` at the file-level trailing position (i.e. at the existing line 685)
    - `public/index.html` has zero direct calls to `navigator.clipboard.writeText` (all four sites go through `copyToClipboard`)
    - `public/index.html` line 214 (or its replacement) renders the permalink as an `<a id="permalink-link">` anchor styled as a code-style inline text element, with the existing Copy button kept as a small `.button-tiny` next to it
    - `public/style.css` adds a `.result-permalink__url` class that styles the permalink anchor as a readable monospace code-style link (matching the existing `.result-permalink code` background)
  key_links:
    - `globalThis.openDiffModal = openDiffModal;` inside `initSuggestionsPanel` → `public/index.html` `open-diff-for-latest` listener (around the existing line 1050 region) can call `globalThis.openDiffModal(latest, null, slug)` without the 2-second "Diff modal not yet loaded" fallback firing
    - `copyToClipboard(text)` helper → `navigator.clipboard.writeText(text)` when `window.isSecureContext` is true, otherwise `document.execCommand('copy')` via a temporary off-screen `<textarea>` → all four Copy button call sites (perm + 4 artifacts + folder path + session id) work on HTTP LAN origins
    - The result block permalink anchor (`#permalink-link`) → `.result-permalink__url` CSS class → renders as a monospace text link so the user perceives it as a URL, not a button

---

<objective>
Close the three gaps surfaced by the Phase 3 UAT (G-03-1, G-03-2, G-03-3). The fixes are surgical, client-side only, and confined to a single deliverable: a working result block that visually reads as a permalink + clean copy buttons + a working diff modal entry point.

G-03-1 (cosmetic): the result block's permalink display should look like a clickable anchor with a small Copy button, not a button-driven control. The HTML at line 214 changes to give the `<a>` a code-style monospace appearance via a new `.result-permalink__url` class.

G-03-2 (blocker): `public/suggestions.js` throws `ReferenceError: openDiffModal is not defined` on page load because `globalThis.openDiffModal = openDiffModal;` is at file-level (line 685) outside the IIFE that defines `openDiffModal`. Move the assignment INSIDE the IIFE so the identifier is in scope. The function MUST stay inside the IIFE — its closure-scoped state (diffModalTrigger, currentDiffSlug, etc.) is not exportable.

G-03-3 (major): the four new Copy buttons (permalink + 4 artifact links) call `navigator.clipboard.writeText` which rejects on non-secure-context HTTP origins. Add a `copyToClipboard(text)` helper that prefers the Async Clipboard API on secure contexts and falls back to `document.execCommand('copy')` via a temporary off-screen `<textarea>` on insecure contexts. Apply the helper to the four new call sites AND the two existing call sites (Folder Path Copy, OpenCode session-id Copy) for consistency.

Purpose: ship the three gap fixes from the Phase 3 UAT so the result block is fully functional on a non-localhost HTTP origin (the user's LAN setup).
Output: working diff modal entry point, working Copy buttons, and a visually clean permalink display.
</objective>

<execution_context>
@/home/adf_home_joel/.local/share/opencode/worktree/2b8a1538785307c7a14ebb52a3dfa52bc3d2b581/feat/permalink-prefill/.planning/phases/03-form-prefill-permalink-urls-and-enhanced-job-folder-links/03-UAT.md
@/home/adf_home_joel/.local/share/opencode/worktree/2b8a1538785307c7a14ebb52a3dfa52bc3d2b581/feat/permalink-prefill/.planning/phases/03-form-prefill-permalink-urls-and-enhanced-job-folder-links/03-01-SUMMARY.md
@/home/adf_home_joel/.local/share/opencode/worktree/2b8a1538785307c7a14ebb52a3dfa52bc3d2b581/feat/permalink-prefill/.planning/phases/03-form-prefill-permalink-urls-and-enhanced-job-folder-links/03-02-SUMMARY.md
</execution_context>

<context>
@public/index.html §"#result" (lines 206-220) — the result block to restyle for G-03-1
@public/index.html §"session-id-copy click handler" (lines 244-257) — the first of two existing `navigator.clipboard.writeText` call sites to migrate to the helper
@public/index.html §"copy-path-btn click handler" (lines 1061-1068) — the second existing call site to migrate
@public/index.html §"renderResultBlock" (lines 825-893) — the four artifact copy buttons (lines 851-862) and the permalink copy button (lines 875-882) to migrate
@public/index.html §"initConfig / buildPermalink / setPermalinkHash / runPrefill / renderResultBlock" (lines 741-893) — defines the `__JOBS_PATH__`, `lastJobDir`, `buildPermalink`, `setPermalinkHash` helpers used downstream
@public/suggestions.js §"initSuggestionsPanel" (lines 88-669) — the IIFE; `openDiffModal` is declared at line 316 and the IIFE closes at line 669. The `globalThis.openDiffModal = openDiffModal;` line is at file-level on line 685, outside the IIFE.
@public/style.css §".result-permalink" (lines 566-568) — existing styles to extend; the new `.result-permalink__url` class belongs adjacent to these
@public/style.css §".button-tiny" (line 227) — existing class for the small Copy button next to the permalink
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix openDiffModal ReferenceError, add copyToClipboard helper, restyle permalink anchor, migrate all four call sites, update FEATURES.md</name>
  <files>public/index.html, public/suggestions.js, public/style.css, docs/FEATURES.md</files>
  <read_first>
@public/suggestions.js (lines 316-341 for the `openDiffModal` function declaration; line 669 for the IIFE closing brace; line 685 for the file-level `globalThis.openDiffModal = openDiffModal;` to remove)
@public/index.html (line 214 for the permalink markup; lines 245-257 for the session-id copy handler; lines 825-893 for renderResultBlock with the four artifact copy buttons and the permalink copy handler; lines 1061-1068 for the copy-path-btn handler; lines 695-740 for the IIFE top, where the `copyToClipboard` helper should be inserted)
@public/style.css (lines 566-568 for the existing `.result-permalink` styles; line 227 for the existing `.button-tiny` style to keep the Copy button consistent)
@docs/FEATURES.md (lines 25-35 for the existing "Permalink URL" / "Result block" bullets to extend)
  </read_first>
  <action>
This task is a single combined edit because the three gaps share a "client-side UI polish" theme, all touch `public/index.html`, and the `copyToClipboard` helper and the permalink visual change must land atomically (separating them would leave the permalink still pointing at the old `navigator.clipboard.writeText` call site during the roll-out). Treat each sub-step as a labeled commit, but execute them in one task.

**Sub-step A — Fix the `openDiffModal` ReferenceError (G-03-2) in `public/suggestions.js`:**

1. Open `public/suggestions.js` and confirm the layout: `initSuggestionsPanel` begins at line 88, `async function openDiffModal(version, trigger, explicitSlug)` is declared at line 316 inside the IIFE, and the IIFE closes with `}` at line 669. The line at 685 — `globalThis.openDiffModal = openDiffModal;` — sits at file-level, outside the IIFE, which is why `openDiffModal` is not in scope.
2. Remove the line at 685 (`globalThis.openDiffModal = openDiffModal;`). Do not replace it with anything at file-level.
3. Add `globalThis.openDiffModal = openDiffModal;` INSIDE the IIFE, immediately after the closing `}` of the `openDiffModal` function (which ends at the existing line 341). Place the new line on its own at the same indent as `closeDiffModal` (which begins at line 343). This puts the assignment inside the function-scoped `initSuggestionsPanel` where `openDiffModal` is in scope.
4. Constraint: do NOT move the `openDiffModal` function out of the IIFE. It closes over `diffModalTrigger`, `currentDiffSlug`, `currentDiffVersion`, `cachedWordDiffHtml`, `diffTitle`, `diffPre`, `renderDiffPaths`, `setStatus`, `fetchDiff`, `renderDiffResponse`, `diffModalRoot`, `diffCloseBtn`, `statusNode`, `getJobSlug` — moving the function out would lose all of these.

**Sub-step B — Add `copyToClipboard` helper to `public/index.html`:**

1. Insert a single `async function copyToClipboard(text) { ... }` helper near the top of the inline `<script>` block. The block begins at the existing line 223; place the helper just after the `// Resume type selection` block (around line 242), BEFORE the session-id-copy handler. This places it at module-init time so every later call site can use it.
2. The helper signature: `async function copyToClipboard(text: string): Promise<boolean>`. Returns `true` on success, `false` on failure.
3. The helper body: try the Async Clipboard API path first (`navigator.clipboard && window.isSecureContext` → `await navigator.clipboard.writeText(text)`), `return true` on success. On any thrown error OR when `navigator.clipboard` is unavailable OR `window.isSecureContext` is false, fall through to a temporary `<textarea>` approach: create `<textarea>`, set its value to `text`, set its style to `position: fixed` and `opacity: 0` (so it does not flash on screen), append to `document.body`, focus, select, call `document.execCommand('copy')`, remove the textarea, return the result of `execCommand('copy')`. Wrap the fallback in `try { ... } catch (e) { return false; }`.

**Sub-step C — Migrate all four call sites to use `copyToClipboard`:**

1. Site 1 (line 245-257, `session-id-copy` click handler): replace the `await navigator.clipboard.writeText(value);` call with `const ok = await copyToClipboard(value);`. Keep the existing "Copied!" / "Copy" / 1200 ms setTimeout dance; do not change the UI. Drop the `console.error` since the helper logs internally — or keep it but make it conditional on `!ok`. (Keep it simple: keep the existing console.error so a failure to copy is still diagnosable.)
2. Site 2 (line 855-862, artifact copy button click handler inside `renderResultBlock`): replace `await navigator.clipboard.writeText(absolute);` with `const ok = await copyToClipboard(absolute);`. Keep the "Copied!" / "Copy" / 1200 ms setTimeout dance.
3. Site 3 (line 876-882, permalink copy button click handler inside `renderResultBlock`): replace `await navigator.clipboard.writeText(url);` with `const ok = await copyToClipboard(url);`. Keep the "Copied!" / "Copy" / 1200 ms setTimeout dance.
4. Site 4 (line 1062-1068, `copy-path-btn` click handler): the existing handler uses `.then(...)` and does not have a try/catch. Refactor to `async` so the helper can be awaited: `document.getElementById('copy-path-btn').addEventListener('click', async () => { const ok = await copyToClipboard(folderPathInput.value); if (!ok) return; const copyBtn = document.getElementById('copy-path-btn'); copyBtn.textContent = 'Copied!'; setTimeout(() => { copyBtn.textContent = 'Copy'; }, 2000); });` (note: 2000 ms, not 1200 ms — the existing handler uses 2 seconds; do not change the timing).
5. Constraint: the file should now have ZERO direct calls to `navigator.clipboard.writeText` — only the helper references it.

**Sub-step D — Restyle the permalink anchor (G-03-1) in `public/index.html` and `public/style.css`:**

1. In `public/index.html` line 214, change the permalink paragraph from:
   `<p id="result-permalink" class="result-permalink hidden">Permalink: <a id="permalink-link" href="#" target="_blank"></a> <button type="button" id="copy-permalink-btn" class="button-tiny">Copy</button></p>`
   to:
   `<p id="result-permalink" class="result-permalink hidden">Permalink: <a id="permalink-link" class="result-permalink__url" href="#" target="_blank" rel="noopener noreferrer"></a> <button type="button" id="copy-permalink-btn" class="button-tiny">Copy</button></p>`
   The structural change is adding the `class="result-permalink__url"` to the anchor and adding `rel="noopener noreferrer"` for safe `target="_blank"`. The anchor's `textContent` is set in `renderResultBlock` (existing line 873: `permalinkLink.textContent = url;`) — do NOT change that line.
2. In `public/style.css`, immediately after the existing `.result-permalink code { ... }` rule (around line 567-568), add: `.result-permalink__url { font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; font-size: 0.95em; background: #1a1a2e; color: #e0e0e0; padding: 2px 6px; border-radius: 3px; word-break: break-all; text-decoration: none; }` and `.result-permalink__url:hover { background: #2a2a4e; color: #fff; }`. These mirror the existing `.result-permalink code` background so the URL visually reads as code text and the user perceives it as a URL, not a button.

**Sub-step E — Documentation update (G-03-1 is a user-noticeable visual change):**

1. In `docs/FEATURES.md`, find the existing "Permalink URL" bullet (around line 25-27) and append one sentence at the end: "The permalink is rendered as a monospace code-style anchor with a small Copy button next to it." This matches the AGENTS.md "Keep Docs in Sync With Code" rule for user-noticeable visual changes.
2. Do NOT update `README.md` — the existing "Permalink URLs and form prefill" section (lines 135-161) describes the URL format and the Copy button in prose but does NOT describe the button-driven UI as a feature. The visual change is a refinement, not a documented behavior change.

**Sub-step F — Verification:**

1. Run `grep -c "navigator.clipboard.writeText" public/index.html` — MUST return 0. If non-zero, search for missed call sites.
2. Run `grep -c "globalThis.openDiffModal = openDiffModal;" public/suggestions.js` — MUST return 1.
3. Run `grep -n "globalThis.openDiffModal" public/suggestions.js` — the line MUST be inside the IIFE (i.e. line number between 88 and 669), NOT at file level (line 685).
4. Run `grep -c "function copyToClipboard" public/index.html` — MUST return 1.
5. Run `grep -c "result-permalink__url" public/index.html` — MUST return at least 1 (the new class on the anchor in the markup).
6. Run `grep -c "result-permalink__url" public/style.css` — MUST return at least 1 (the new CSS rule).
7. Run `grep -c "result-permalink__url" docs/FEATURES.md` — MUST return 0 (the new class name is not user-facing; the doc text describes the behavior in prose).
8. Run `npm test` — must be clean. These changes are client-side UI only; the existing server-side Vitest suite is unaffected.
9. Run `npm run build` — must be clean.
10. Run `pre_commit_code_health_safeguard` on the changed files if the codescene CLI is on `PATH`. If unavailable, do a manual diff review of the changes for code health (style consistency, no dead code, no over-large functions).
11. Create `.planning/phases/03-form-prefill-permalink-urls-and-enhanced-job-folder-links/03-03-SUMMARY.md` summarising the three gap fixes per the standard SUMMARY template: what shipped (per file), verification evidence (the grep counts above + npm test + npm run build), deviations from the plan, and self-check.

**Sub-step G — Manual runbook for the executor (since these are client-side UI behaviors, the test suite cannot exercise them):**

The executor must execute the following on a running dev server (`npm start`) on the user's LAN (e.g. `http://192.168.x.x:3000` — a non-secure-context origin). Record the outcomes in `03-03-SUMMARY.md`:

1. Load the page; open DevTools console. Confirm there is NO `ReferenceError: openDiffModal is not defined` log.
2. Generate a resume (or load an existing permalink). Confirm the result block appears.
3. Visually inspect the permalink line. It should read as a monospace code-style text link, not a button.
4. Click the "Copy" button next to the permalink. Confirm the clipboard now contains the permalink URL and the button shows "Copied!" for 1.2 seconds.
5. Click each of the four artifact Copy buttons in turn. Confirm the clipboard contains the absolute URL of that artifact and the button shows "Copied!" for 1.2 seconds.
6. Click "Compare with latest backup". Confirm the diff modal opens (not the "Diff modal not yet loaded" toast). Close the modal.
7. Type a folder path into `#folderPath` and click the Folder Path "Copy" button. Confirm the clipboard contains the folder path.
8. If a session id has been generated, click the session-id Copy button. Confirm the clipboard contains the session id.
9. Run the manual runbook on `localhost` (a secure context) and confirm Copy buttons still work there too (no regression).
  </action>
  <verify>
<automated>grep -c "navigator.clipboard.writeText" public/index.html | grep -q '^0$' && grep -c "globalThis.openDiffModal = openDiffModal;" public/suggestions.js | grep -q '^1$' && grep -c "function copyToClipboard" public/index.html | grep -q '^1$' && grep -c "result-permalink__url" public/index.html | grep -q -E '^[1-9][0-9]*$' && grep -c "result-permalink__url" public/style.css | grep -q -E '^[1-9][0-9]*$' && npm test --silent && npm run build --silent</automated>
# All grep counts must be exact. The combined command short-circuits on the first failure so a single failed assertion surfaces clearly in the executor's output. `npm test` and `npm run build` confirm the server-side suite is still clean (these are client-side UI changes, so the server suite should be unaffected, but it is a cheap regression check).
  </verify>
  <done>
- `public/suggestions.js` has exactly one `globalThis.openDiffModal = openDiffModal;` line, located inside the `initSuggestionsPanel` IIFE (line number between 88 and 669 inclusive). The previous file-level line at 685 is gone.
- `public/index.html` has exactly one `function copyToClipboard` declaration, near the top of the inline script (before the session-id-copy handler at the existing line 244).
- `public/index.html` has zero direct calls to `navigator.clipboard.writeText`. All four call sites (session-id-copy at line 245, artifact copy buttons at line 855, permalink copy button at line 876, copy-path-btn at line 1062) call `copyToClipboard(...)` instead.
- `public/index.html` line 214 renders the permalink anchor with `class="result-permalink__url"` and `rel="noopener noreferrer"`.
- `public/style.css` has a `.result-permalink__url` rule styled as monospace text with a `#1a1a2e` background (matching the existing `.result-permalink code` background) and a `:hover` state.
- `docs/FEATURES.md` "Permalink URL" bullet has a trailing sentence describing the monospace code-style anchor with Copy button.
- The page loads with no `ReferenceError: openDiffModal is not defined` in the browser console.
- "Compare with latest backup" opens the diff modal directly (not the "Diff modal not yet loaded" toast).
- All four Copy buttons (perm + 4 artifacts + folder path + session id) work on a non-secure-context HTTP origin (the user's LAN setup).
- `npm test` is clean. `npm run build` is clean.
- The `pre_commit_code_health_safeguard` (if available) is clean, or a manual diff review confirms no code-health regression.
- `03-03-SUMMARY.md` is created and summarises the three gap fixes.
  </done>
</task>

</tasks>

<verification>
- `grep -c "navigator.clipboard.writeText" public/index.html` returns 0
- `grep -c "globalThis.openDiffModal = openDiffModal;" public/suggestions.js` returns 1
- `grep -n "globalThis.openDiffModal" public/suggestions.js` shows the line is at a line number inside the `initSuggestionsPanel` IIFE (i.e. < 669), not at the previous file-level line 685
- `grep -c "function copyToClipboard" public/index.html` returns 1
- `grep -c "result-permalink__url" public/index.html` returns ≥ 1
- `grep -c "result-permalink__url" public/style.css` returns ≥ 1
- `npm test` is clean
- `npm run build` is clean
- `pre_commit_code_health_safeguard` (if available) reports no regression
- Manual runbook steps 1-9 from the action block all pass on a non-secure-context HTTP origin
</verification>

<success_criteria>
- The three UAT gaps G-03-1, G-03-2, G-03-3 are closed
- The result block permalink is visually a clean anchor link (not a button-driven control)
- The "Compare with latest backup" button opens the diff modal without the "Diff modal not yet loaded" toast
- All four new Copy buttons + the two existing Copy buttons work on a non-secure-context HTTP origin
- The fix is minimal: 3 files modified (`public/index.html`, `public/suggestions.js`, `public/style.css`) + 1 doc update (`docs/FEATURES.md`) + 1 SUMMARY file
- No new server routes, no new dependencies, no test-suite changes
- `npm test` and `npm run build` are clean
</success_criteria>

<output>
Create `.planning/phases/03-form-prefill-permalink-urls-and-enhanced-job-folder-links/03-03-SUMMARY.md` when done. The SUMMARY should list the three gap fixes (G-03-1, G-03-2, G-03-3), the grep-based verification evidence, the npm test + build outcomes, and a manual-runbook section.
</output>
