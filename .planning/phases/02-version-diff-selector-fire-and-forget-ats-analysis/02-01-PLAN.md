---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - public/suggestions.js
  - public/style.css
  - services/backupService.ts
autonomous: true
requirements:
  - VDS-01
  - VDS-02
  - VDS-03
  - VDS-04
  - UI-01
  - UI-02

must_haves:
  truths:
    - User can select any job folder via autocomplete text input
    - Version dropdowns show all available backup versions (v1, v2, ...) for selected folder
    - Clicking "Diff" reuses the existing suggestions-diff-modal to show unified diff + changed paths
    - Main model fills most of the screen
    - Word diff shows line numbers when text overflows
  artifacts:
    - Version-diff-selector panel in index.html below jd-search-panel
    - `GET /generate/listJobDirs` endpoint listing all job folder slugs
    - `listBackupVersions(backupsRoot)` in backupService.ts
    - `wrapDiffLinesWithSpans` adds line numbers to diff output
  key_links:
    - index.html panel → suggestions.js version-loading logic
    - suggestions.js → GET /generate/diffResume (existing, D-04)
    - index.html model textarea → style.css UI tweak
---

<objective>
Add a version-diff-selector panel to the main UI and fix two Phase 1 UI nits. The panel lets the user pick any job folder and diff any two backup versions, reusing the existing diff modal. Two Phase 1 UI tweaks are also folded in: the main model textarea fills most of the screen, and the word-diff output shows line numbers.
</objective>

<execution_context>
@/home/adf_home_joel/src/copilot/resume-opencode/.planning/phases/02-version-diff-selector-fire-and-forget-ats-analysis/02-CONTEXT.md
</execution_context>

<context>
@public/index.html §"Search past JDs" (lines 138-156) — reference pattern for new panel
@public/index.html §"JD search setup" (lines 690-765) — autocomplete fetch pattern
@public/suggestions.js §"openDiffModal" (lines 309-334) — modal open + diff fetch + render
@public/suggestions.js §"wrapDiffLinesWithSpans" (lines 296-307) — add line numbers here
@public/style.css §".jd-search" (lines 348-410) — reference styles for new panel
@public/suggestions.html §"suggestions-diff-modal" (lines 56-75) — existing modal scaffold
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add listBackupVersions + listJobDirs helpers + GET /generate/listJobDirs</name>
  <files>services/backupService.ts, routes/generate.ts</files>
  <action>
Add `listBackupVersions(backupsRoot: string): number[]` to `services/backupService.ts`. It reads `backupsRoot`, enumerates `v\d+` directories, returns sorted array of version numbers (oldest first). Reuse the same regex pattern as `latestBackupVersion`.

Add `listJobDirs(jobsRoot: string): string[]` to `services/backupService.ts`. It reads `jobsRoot`, returns subdirectory names sorted alphabetically (filtering to directories only).

In `routes/generate.ts`, add `GET /generate/listJobDirs` handler: reads `jobsRoot` via `ensureJobsRootRealpath`, calls `listJobDirs`, returns `{ dirs: string[] }`. No auth required (same pattern as `/generate/listJobFiles`).

Do NOT add an endpoint for listing backup versions of a specific folder — that will be done inline in suggestions.js by reading `fs.readdirSync` on the selected folder's backups dir directly from the client via a new tiny endpoint. Add `GET /generate/listBackups?jobDir=…` that calls `listBackupVersions` and returns `{ versions: number[] }`.

</action>
  <verify>
<automated>curl -s http://localhost:3000/generate/listJobDirs | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log(Array.isArray(d.dirs) ? 'OK: '+d.dirs.length+' dirs' : 'FAIL')"</automated>
</verify>
  <done>`GET /generate/listJobDirs` returns array of job slugs; `GET /generate/listBackups?jobDir=…` returns `{versions:[1,2,3]}` sorted ascending</done>
</task>

<task type="auto">
  <name>Task 2: Add version-diff-selector panel to index.html</name>
  <files>public/index.html</files>
  <action>
In `public/index.html`, below the `jd-search-panel` section (after line 156), add a new `<section id="version-diff-panel" class="jd-search">` using the same visual class as the JD search panel per D-01.

The panel contains (per D-01, D-02, D-03):
- Heading: "Diff two backup versions"
- Subtitle: "Select any job folder and compare two saved versions of your resume."
- A text input with datalist for folder autocomplete: `<input type="text" id="vds-folder-input" list="vds-folder-list" placeholder="e.g. shopify-senior-se-2025-06-18">` + `<datalist id="vds-folder-list">` (populated by JS from `GET /generate/listJobDirs` on page load).
- Two `<select id="vds-source-version">` and `<select id="vds-target-version">` dropdowns, both initially disabled/empty.
- A "Diff" button `<button type="button" id="vds-diff-btn" class="button button-secondary single-button" disabled>Diff</button>`.
- Wire the vds-folder-input so that on change, it fetches `GET /generate/listBackups?jobDir=…` and populates both version dropdowns with `v1`, `v2`, etc. options. Populate both dropdowns identically since any two can be compared.
- Pre-fill the folder input with `globalThis.__resumeOpencode.lastJobDir` if set.

Style: reuse the `.jd-search` CSS class already defined in `style.css`. No new CSS framework.

</action>
  <verify>
<automated>grep -c 'id="version-diff-panel"' public/index.html && grep -c 'vds-folder-input' public/index.html && grep -c 'vds-source-version' public/index.html</automated>
</verify>
  <done>New panel renders below JD search panel with folder input, two version dropdowns, and disabled Diff button</done>
</task>

<task type="auto">
  <name>Task 3: Wire version-diff-selector to existing diff modal in suggestions.js</name>
  <files>public/suggestions.js</files>
  <action>
In `public/suggestions.js` (inside `initSuggestionsPanel` or as a standalone IIFE at the bottom), add a handler for the version diff selector panel:

1. On page load, fetch `GET /generate/listJobDirs` and populate the `<datalist id="vds-folder-list">` with `<option>` elements for each slug.

2. On `vds-folder-input` change: fetch `GET /generate/listBackups?jobDir=<slug>`, populate both version dropdowns (enable them once versions exist), and pre-select the last two versions (highest two).

3. On `vds-diff-btn` click: read the folder and two selected versions, then call `openDiffModal(sourceVersion, vdsDiffBtn)` — the existing `openDiffModal` already fetches `GET /generate/diffResume?jobDir=…&version=…&format=both`. Update `currentDiffSlug` and `currentDiffVersion` before calling so the modal title shows the correct folder.

Note: The existing `openDiffModal` gets slug via `getJobSlug()` (globalThis.__resumeOpencode.lastJobDir). For the version-diff panel we have an explicit folder input, so modify `openDiffModal` to accept an optional `(version, trigger, explicitSlug)` third argument — if `explicitSlug` is passed, use it instead of `getJobSlug()`.

The existing backup-link trigger in suggestions panel (lines 419-428) continues to use `openDiffModal(backupVersion, trigger)` without explicitSlug.

</action>
  <verify>
<automated>grep -c 'vds-folder-input\|vds-diff-btn\|vds-source-version' public/suggestions.js</automated>
</verify>
  <done>Typing a folder in the vds input populates version dropdowns; clicking Diff opens the diff modal with the correct two versions</done>
</task>

<task type="auto">
  <name>Task 4: Phase 1 UI tweaks — main model fills screen + line numbers in word diff</name>
  <files>public/style.css, public/suggestions.js</files>
  <action>
**D-12 — Main model textarea fills most of screen:**
In `public/style.css`, add a rule for `#jobDescription` that gives it `min-height: 60vh` and `flex: 1` within the form. Alternatively (if the layout allows), increase `rows="14"` to `rows="20"` in `index.html` line 47. Choose whichever makes the textarea visually dominant. The goal is that when the page loads, the JD textarea is the most visually prominent element (aside from the main Generate button).

**D-13 — Word diff shows highlighted line numbers:**
In `public/suggestions.js` `wrapDiffLinesWithSpans` (lines 296-307), modify to:
1. Track which lines contain diff change markers (elements with class `change`, `add`, or `remove` — same markers used by the unified diff output)
2. Prefix each output line with its 1-based line number in a left gutter column
3. **If that line has changes**, wrap the line number in green: `<span class="diff-line-num diff-line-num--changed">${lineNum}</span>`
4. **If that line has no changes**, use the muted style: `<span class="diff-line-num">${lineNum}</span>`
5. Add mobile soft-wrap: `.diff-pre { white-space: pre-wrap; word-break: break-word; overflow-wrap: break-word; }`

Add CSS in `style.css`:
```css
.diff-line-num { color: #555; user-select: none; margin-right: 0.5rem; min-width: 2rem; display: inline-block; }
.diff-line-num--changed { color: #16a34a; font-weight: 600; }
.diff-pre { white-space: pre-wrap; word-break: break-word; overflow-wrap: break-word; }
```
Update the diff modal `<pre>` to have class `diff-pre` (confirm it already has `diff-pre` from the HTML, just ensure the soft-wrap rule is added to `style.css`).

</action>
  <verify>
<automated>grep -c 'diff-line-num' public/style.css && node -e "const fs=require('fs'); const c=fs.readFileSync('public/suggestions.js','utf8'); console.log(c.includes('diff-line-num') ? 'OK' : 'MISSING')"</automated>
</verify>
  <done>JD textarea is visually dominant; diff lines are prefixed with line numbers in the modal</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /generate/listJobDirs | Unauthenticated; serves job folder slugs only (already accessible via job-description search) |
| client → GET /generate/listBackups | Unauthenticated; serves version list for a specific job folder |
| client → GET /generate/diffResume | Existing endpoint — read-only, path-allowlisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | GET /generate/listJobDirs | low | accept | Folder slugs are low-sensitivity; JD search already enumerates them |
| T-02-02 | Tampering | version diff panel input | low | accept | Server-side `safeRealpath` + `pathIsInsideDir` on diffResume handles path traversal |
| T-02-SC | Soup | npm installs | N/A | N/A | No new packages in this plan |

</threat_model>

<verification>
## Plan-level checks

1. `GET /generate/listJobDirs` returns 200 with `{dirs: [...]}`
2. `GET /generate/listBackups?jobDir=<slug>` returns `{versions: [1,2,...]}`
3. New panel visible in DOM below jd-search-panel
4. Folder autocomplete populates on page load
5. Version dropdowns populate when folder selected
6. Diff button opens diff modal with correct two versions
7. JD textarea is visually larger (min 60vh or rows=20)
8. Diff modal shows line numbers on each diff line
</verification>

<success_criteria>
Version-diff-selector panel is functional: user can select any job folder, pick any two backup versions, and see the unified diff in the existing modal. Phase 1 UI tweaks are also complete: main model textarea is visually prominent, and diff lines show line numbers.
</success_criteria>

<output>
Create `.planning/phases/02-version-diff-selector-fire-and-forget-ats-analysis/02-01-SUMMARY.md` when done
</output>
